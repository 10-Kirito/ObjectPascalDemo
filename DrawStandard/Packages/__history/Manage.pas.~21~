unit Manage;

interface

uses
  Command, GraphicBasic, Graphics;

type
  TManager = class
  private
    FBitMap: TBitmap;
    FGraphicReceiver: TGraphicReceiver;
  public
    constructor Create(ABitMap: TBitmap);

  end;

implementation

{ TManager }

constructor TManager.Create(ACanvas: TCanvas);
begin
  FCanvas := ACanvas;
  FGraphicReceiver := TGraphicReceiver.Create(FCanvas);

  // create commands
end;

end.
